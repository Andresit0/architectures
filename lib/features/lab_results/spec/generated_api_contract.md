# Generated API Contract — Lab Results

> Auto-generated by app-agent-api-extractor at Phase D.0.5.
> Source: 6 spec files in spec/. Do NOT edit manually — re-run Phase D.0.5 if changes needed.
>
> NOTE (documented deviation): this file was hand-updated to reflect the refactor that merged `filterByPeriod`
> into `period.dart` (co-located with the `Period` enum, pattern `deriveLabResultStatus`) and removed the
> unused `labelKey` getter. Re-running Phase D.0.5 against the updated domain.md/tasks.md is equivalent.

## Section 1: Entities

All domain entities are **REUSED from the Shared Kernel** (`lib/shared/models/lab_results/`), persisted by `core/database` (`LabResultsSerializer`). The feature MUST NOT create new entity files. The only feature-owned model is the `Period` value object.

DTO needed: YES for the three freezed entities (contracts.md shows the HTTP response mapped via feature-private wire DTOs in `lib/features/lab_results/infrastructure/dtos/`). Enums have no DTO.

### LabResultEntity
`reused: true` — location `lib/shared/models/lab_results/lab_result_entity.dart` (@freezed, `const ._()`)
| Field | Type | Required |
|-------|------|----------|
| id | String | yes |
| testCode | String | yes |
| testName | String | yes |
| category | String | yes |
| unit | String? | no |
| kind | LabResultKind | yes |
| referenceRange | LabResultReferenceRangeEntity? | no |
| values | List<LabResultValueEntity> | yes |

Notes: Exposes a `latestValue` getter → `LabResultValueEntity?` (most recent date; null when values is empty) and a `status` getter → `LabResultStatus` derived from `latestValue` vs referenceRange (additive; schema unchanged). `kind == LabResultKind.text` → values carry textValue; `kind == LabResultKind.numeric` → values carry value. Imported via `package:clean_architecture_sdd_harness/shared/models/_models.lib.dart`.

### LabResultValueEntity
`reused: true` — location `lib/shared/models/lab_results/lab_result_value_entity.dart` (@freezed, `const ._()`)
| Field | Type | Required |
|-------|------|----------|
| date | DateTime | yes |
| value | double? | no |
| textValue | String? | no |

Notes: Exactly one of value/textValue is non-null per the parent result's kind. Enforced by the mapper/serializer round-trip.

### LabResultReferenceRangeEntity
`reused: true` — location `lib/shared/models/lab_results/lab_result_reference_range_entity.dart` (@freezed, `const ._()`)
| Field | Type | Required |
|-------|------|----------|
| low | double | yes |
| high | double | yes |

Notes: null on the parent result → no chart band, status Unknown.

### LabResultKind
`reused: true` — location `lib/shared/models/lab_results/lab_result_kind.dart` (enum, pure Dart, no codegen)
Values: `numeric` / `text`. Raw wire values "numeric" | "text", unknown falls back to text. No fields.

### LabResultStatus
`reused: true` — location `lib/shared/models/lab_results/lab_result_status.dart` (enum, pure Dart, no codegen)
Values: `normal` / `high` / `low` / `unknown`. Pure function `deriveLabResultStatus(double? value, LabResultReferenceRangeEntity? range)` in the same file: value > high → high; value < low → low; within (inclusive bounds) → normal; no range → unknown. UI localizes the label via l10n, never hardcodes.

### Period
`reused: false` — location `lib/features/lab_results/domain/value_objects/period.dart` (enum, pure Dart, no codegen, no Result)
Values: `threeMonths` / `sixMonths` / `oneYear` / `all` (default all). Provides a duration used by `filterByPeriod`, whose pure function is CO-LOCATED in the same file (pattern: `deriveLabResultStatus` in `lab_result_status.dart`). UI localizes the label via l10n, never hardcodes. Feature-only (no other bounded context consumes it).

## Section 2: Method Signatures

### ILabResultsRemoteDatasource (domain/datasources)
`lib/features/lab_results/domain/datasources/i_lab_results_remote_datasource.dart`
- loadRemote() → Future<List<LabResultEntity>>

### ILabResultsLocalDatasource (domain/datasources)
`lib/features/lab_results/domain/datasources/i_lab_results_local_datasource.dart`
- loadLocal() → Future<List<LabResultEntity>>
- storeLocal(List<LabResultEntity> entities) → Future<void>

### ILabResultsRepository (domain/repositories)
`lib/features/lab_results/domain/repositories/i_lab_results_repository.dart`
- loadLabResults() → Future<Result<List<LabResultEntity>>>
- refreshLabResults() → Future<Result<List<LabResultEntity>>>

### LoadLabResultsUseCase (domain/usecases)
`lib/features/lab_results/domain/usecases/load_lab_results_usecase.dart` — constructor arg: `repository: ILabResultsRepository`
- call() → Future<Result<List<LabResultEntity>>>

### RefreshLabResultsUseCase (domain/usecases)
`lib/features/lab_results/domain/usecases/refresh_lab_results_usecase.dart` — constructor arg: `repository: ILabResultsRepository`
- call() → Future<Result<List<LabResultEntity>>>

### Pure functions (domain/value_objects + shared kernel)
- `List<LabResultValueEntity> filterByPeriod(List<LabResultValueEntity> values, Period period)` — `lib/features/lab_results/domain/value_objects/period.dart` (co-located with the `Period` enum). Filters relative to the MOST RECENT data date of the full unfiltered list; keeps values within [mostRecent - period.duration, mostRecent]; Period.all (or a null duration) returns values unchanged. Never applied to the card's latest value/status chip.
- `LabResultStatus deriveLabResultStatus(double? value, LabResultReferenceRangeEntity? range)` — `lib/shared/models/lab_results/lab_result_status.dart`.
- `LabResultStatus get status` — additive getter on `LabResultEntity` (latest value wins).

### Referenced seams (NOT created inside the feature)
- `ILabResultsStore` — `lib/shared/interfaces/i_lab_results_store.dart` (ISP split: `ILabResultsReader.loadAll`, `ILabResultsWriter.storeAll/deleteAll`, `ILabResultsStore implements both`). Adapter over it in `LabResultsLocalDatasourceImpl`. It never clears the cache — deletion only via account reset.
- `ITrendChart` — `lib/core/services/charts/fl_chart_wrapper.dart` (created at D.0.6 wrapper TDD). API finalized in the wrapper spike and captured here; minimum surface renders a line chart with a reference-range band and legible axes. Accessed via `trendChartProvider`; feature code NEVER imports `package:fl_chart`.

## Section 3: State Variants

### LabResultsState (sealed @freezed — NO `._()` private constructor)
`lib/features/lab_results/presentation/notifiers/lab_results_state.dart`
- LabResultsInitial()
- LabResultsLoading()
- LabResultsLoaded({ required List<LabResultEntity> results, String? selectedTestId, Period period })
- LabResultsFailure({ required AppError error })

Notifier: `LabResultsNotifier` (`@riverpod class`, `Notifier<LabResultsState>`). build() → LabResultsInitial. `load()`/`refresh()` fold Results; `selectTest(String id)`/`setPeriod(Period p)` mutate the Loaded state WITHOUT reloading. After a load/refresh the notifier re-validates `selectedTestId` against the new set (falls back to the first numeric test, or null when none remain).

### labResultsRefreshErrorProvider (presentation UI-state)
`lib/features/lab_results/presentation/notifiers/lab_results_refresh_error_provider.dart` — `Notifier<AppError?>` (code-gen @riverpod class, no dependencies). Transient error emitted when a pull-to-refresh fails while the last loaded results stay visible (mirror of clinicalHistoryRefreshErrorProvider). The list state machine is unchanged: refresh() failure from Loaded keeps results and sets this provider; the screen listens for the snackbar.

## Section 4: Provider Names

Feature DI (`lib/features/lab_results/di/lab_results_provider.dart`):
- datasourceProvider (remote): `_labResultsRemoteDatasourceProvider` (private) — `Provider<ILabResultsRemoteDatasource>` — deps: httpServiceProvider, appUriesProvider
- datasourceProvider (local): `_labResultsLocalDatasourceProvider` (private) — `Provider<ILabResultsLocalDatasource>` — deps: labResultsStoreProvider
- repositoryProvider: `labResultsRepositoryProvider` — `Provider<ILabResultsRepository>` — deps: _labResultsRemoteDatasourceProvider, _labResultsLocalDatasourceProvider
- usecaseProvider: `loadLabResultsUseCaseProvider` — `Provider<LoadLabResultsUseCase>` — deps: labResultsRepositoryProvider
- usecaseProvider: `refreshLabResultsUseCaseProvider` — `Provider<RefreshLabResultsUseCase>` — deps: labResultsRepositoryProvider
- notifierProvider: `labResultsProvider` — Notifier (code-gen @riverpod class, `Notifier<LabResultsState>`) — deps: loadLabResultsUseCaseProvider, refreshLabResultsUseCaseProvider
- refreshErrorProvider: `labResultsRefreshErrorProvider` — `Notifier<AppError?>` (code-gen @riverpod class)

DI re-exports (feature di/ re-exports the seams so presentation never imports packages/core):
- `trendChartProvider` — from `core/services/charts/charts_providers.dart` — `Provider<ITrendChart>`
- `appNavigatorProvider` — from `core/router/app_navigator_provider.dart` — `Provider<IAppNavigator>` — re-exported ONLY if the feature navigates imperatively; EXPECTED NOT needed (back nav is handled by the go_router shell; entry point lives in the clinical history AppBar action).

Shared/core providers consumed (NOT created by the feature):
- `labResultsStoreProvider` — `Provider<ILabResultsStore>` — `lib/core/database/tables/lab_results_providers.dart`
- `httpServiceProvider` — `IDioWrapper` (Bearer + AuthInterceptor) — NOT authDioProvider
- `appUriesProvider` — `AppUries` (adds `labResults` Uri)

## Section 5: BDD Scenarios

1. Load shows numeric cards with status chips and a non-numeric flat list
2. Selecting a numeric test renders its trend chart with a reference-range band
3. All results non-numeric hides the selector and period filter
4. Empty results show an empty state with retry
5. Pull to refresh reloads from the server
6. Pull to refresh failure keeps the loaded results and shows a localized error

## Required Files

### Shared Kernel (created BEFORE feature tests — tasks.md "Shared Kernel")
- lib/shared/models/lab_results/lab_result_kind.dart — enum numeric/text + fromCode fallback to text
- lib/shared/models/lab_results/lab_result_status.dart — enum normal/high/low/unknown + deriveLabResultStatus
- lib/shared/models/lab_results/lab_result_reference_range_entity.dart — @freezed: low, high
- lib/shared/models/lab_results/lab_result_value_entity.dart — @freezed: date, value double?, textValue String?
- lib/shared/models/lab_results/lab_result_entity.dart — @freezed: id, testCode, testName, category, unit?, kind, referenceRange?, values; status getter
- lib/shared/models/_models.lib.dart — export the five (shared/models imported only via the barrel)
- lib/shared/interfaces/i_lab_results_store.dart — ISP split: ILabResultsReader.loadAll, ILabResultsWriter.storeAll/deleteAll, ILabResultsStore implements both
- lib/shared/interfaces/_interfaces.lib.dart — export the store interface

### Core database
- lib/core/database/serializers/lab_results_serializer.dart — LabResultsSerializer.toMap/fromMap (round-trip guarded by test)
- lib/core/database/tables/lab_results.dart — LabResults implements ILabResultsStore; StoreRef 'lab_results'; storeAll replaces all, loadAll maps all, deleteAll clears
- lib/core/database/tables/lab_results_providers.dart — labResultsStoreProvider = Provider<ILabResultsStore>

### Chart wrapper seam (Phase D.0.6 — wrapper TDD before feature tests)
- lib/core/services/charts/fl_chart_wrapper.dart — ITrendChart seam (line chart + reference-range band + legible axes)
- lib/core/services/charts/charts_providers.dart — trendChartProvider = Provider<ITrendChart>

### Core network
- lib/core/network/api_endpoints.dart — add `Uri get labResults` (path: /user/clinical-history/lab-results) + IEndpointConfig (modified)
- lib/core/network/dio_wrapper.dart — map browser unknown / failed-fetch errors to NoConnectionException (conditional, web only)

### Domain (feature — NO entities, reused from shared kernel)
- lib/features/lab_results/domain/datasources/i_lab_results_remote_datasource.dart
- lib/features/lab_results/domain/datasources/i_lab_results_local_datasource.dart
- lib/features/lab_results/domain/repositories/i_lab_results_repository.dart
- lib/features/lab_results/domain/usecases/load_lab_results_usecase.dart
- lib/features/lab_results/domain/usecases/refresh_lab_results_usecase.dart
- lib/features/lab_results/domain/value_objects/period.dart

### Infrastructure
- lib/features/lab_results/infrastructure/dtos/lab_result_dto.dart (+ LabResultValueDto, LabResultReferenceRangeDto) — feature-private, hand-written DTOs (manual fromJson/toJson, no codegen)
- lib/features/lab_results/infrastructure/dtos/lab_results_list_response_dto.dart
- lib/features/lab_results/infrastructure/dtos/lab_results_mapper.dart — LabResultsMapper.fromDtoList (enforces exactly one of value/textValue per kind)
- lib/features/lab_results/infrastructure/datasources/lab_results_remote_datasource_impl.dart — IDioWrapper.get(AppUries().labResults, sla: EndpointSla.standard)
- lib/features/lab_results/infrastructure/datasources/lab_results_local_datasource_impl.dart — adapter over ILabResultsStore
- lib/features/lab_results/infrastructure/repositories/lab_results_repository_impl.dart — fetchOrFallback/guard + best-effort _storeCache

### Presentation
- lib/features/lab_results/presentation/notifiers/lab_results_state.dart — sealed @freezed LabResultsState (Initial/Loading/Loaded/Failure; NO ._())
- lib/features/lab_results/presentation/notifiers/lab_results_refresh_error_provider.dart — Notifier<AppError?>
- lib/features/lab_results/presentation/notifiers/lab_results_notifier.dart — @riverpod class LabResultsNotifier
- lib/features/lab_results/di/lab_results_provider.dart — @riverpod chain + re-export trendChartProvider (appNavigatorProvider only if imperative nav)
- lib/features/lab_results/presentation/utils/lab_value_formatter.dart — formatLabValue (up to 2 decimals, no trailing zeros)
- lib/features/lab_results/presentation/mappers/lab_result_chart_mapper.dart — Entity → TrendChartData (points/xLabels/reference band/tooltips); formatDate/statusLabel/referenceRangeLabel callbacks injected — no l10n leak (presentation-layer mapper, mirror of infrastructure/mappers at the UI boundary)
- lib/features/lab_results/presentation/widgets/lab_results_test_selector.dart — numeric tests only
- lib/features/lab_results/presentation/widgets/lab_results_period_filter.dart — 3 months / 6 months / 1 year / All
- lib/features/lab_results/presentation/widgets/lab_results_card.dart — latest UNFILTERED value + unit + status chip (numeric) / text + no chip (non-numeric)
- lib/features/lab_results/presentation/widgets/lab_results_chart_pane.dart — ITrendChart via trendChartProvider + tooltip (date, value+unit, range, status)
- lib/features/lab_results/presentation/widgets/lab_results_non_numeric_list.dart — flat list of non-numeric tests showing each test's latest text value (period-independent; no filterByPeriod)
- lib/features/lab_results/presentation/screens/lab_results_screen.dart — ConsumerWidget; switch over state; SkeletonList/EmptyState/ErrorState + RefreshIndicator + floating snackbar

### Navigation (Phase D.10)
- lib/shared/router/app_route.dart — add AppRoute.labResults(path: '/clinical-history/lab-results', name: 'lab-results') (modified)
- lib/app/router/app_router.dart — nested GoRoute under AppRoute.clinicalHistory → LabResultsScreen (modified)
- lib/features/clinical_history/presentation/screens/clinical_history_screen.dart — "Lab Results" AppBar action via appNavigatorProvider (modified)

### l10n
- lib/l10n/app_en.arb / lib/l10n/app_es.arb — add labResults + labResultsEmpty + labResultsPeriod* + labResultsStatus* + labResultsChartTitle + labResultsOtherTests + labResultsSelectTest (+ optional keys); then run gen-l10n (modified)

### Tests
- test/core/database/lab_results_serializer_test.dart — round-trip guard
- test/features/lab_results/domain/… — usecase + status derivation tests
- test/features/lab_results/domain/value_objects/… — Period + filterByPeriod tests (period_test.dart)
- test/features/lab_results/infrastructure/… — remote/local datasource + repository tests
- test/features/lab_results/presentation/… — notifier, state, widget tests (mock ITrendChart, never real fl_chart)
- test/features/lab_results/presentation/mappers/lab_result_chart_mapper_test.dart — Entity → TrendChartData: points/xLabels (null filtering), reference band, unit fallback, tooltip (with/without range), callbacks
- test/bdd/lab_results_bdd_test.dart — 6 scenarios via gherkart
- integration_test/lab_results_integration_test.dart — 6 scenarios (fake ILabResultsRepository), executed on device
- test/features/lab_results/presentation/screens/lab_results_screen_golden_test.dart — created at Phase D.8.5, NOT part of the D.0.1–D.0.5b All-Tests-First gate
- test/features/lab_results/presentation/screens/goldens/ — committed fixtures: lab_results_screen_loading.png, lab_results_screen_loaded.png, lab_results_screen_empty.png (created at Phase D.8.5)

## Wrapper API

> For test writers (D.0.1–D.0.5b): **mock the interfaces below — NEVER raw package classes**
> (no `package:fl_chart`, `package:dio`, etc. in feature code or feature tests).

### GROUP 1 — New wrapper created in D.0.6 (fl_chart)

#### FlChartTrendChart — accessed via ref.watch(trendChartProvider)
`FlChartTrendChart` implements `ITrendChart` (`abstract interface class`). Const constructor `const FlChartTrendChart()`. Bound to `trendChartProvider` (`Provider<ITrendChart>`) in `lib/core/services/charts/charts_providers.dart`. Feature code and feature tests import the interface + provider only — never `package:fl_chart`.
- `Widget lineChart({required TrendChartData data})` — renders a `LineChart` with a reference-range band (when both `referenceLow`/`referenceHigh` are non-null), auto-scaled Y axis, bottom x-axis labels, and a touch tooltip joining `tooltipLines` with `\n`.

##### Capacity contract types — `lib/core/services/charts/models/trend_chart_data.dart`
- `TrendPoint({required DateTime date, required double value})` — fields: `date: DateTime`, `value: double`.
- `TrendChartData({required List<TrendPoint> points, List<String> xLabels = const [], double? referenceLow, double? referenceHigh, String unit = '', List<String> tooltipLines = const []})` — fields: `points: List<TrendPoint>`, `xLabels: List<String>`, `referenceLow: double?`, `referenceHigh: double?`, `unit: String`, `tooltipLines: List<String>`.

### GROUP 2 — Existing wrappers/seams used by lab_results

#### IDioWrapper / DioWrapper — accessed via ref.watch(httpServiceProvider)
`IDioWrapper` (`abstract class`) implemented by `DioWrapper`; bound to `httpServiceProvider` (`Provider<IDioWrapper>`, Bearer + AuthInterceptor — NOT `authDioProvider`) in `lib/core/network/dio/dio_providers.dart`. Used by the remote datasource: `IDioWrapper.get(AppUries().labResults, sla: EndpointSla.standard)`. Tests mock `IDioWrapper`.
- `Future<HttpResponse<Map<String, dynamic>>> get(Uri url, {Map<String, String>? headers, String? type, Map<String, String>? pathParams, bool returnDioResponse = false, EndpointSla sla = EndpointSla.unknown})`
- `Future<HttpResponse<Map<String, dynamic>>> post(Uri url, {Map<String, String>? headers, Object? body, String? type, Map<String, String>? pathParams, bool returnDioResponse = false, EndpointSla sla = EndpointSla.unknown})`
- `Future<HttpResponse<Map<String, dynamic>>> patch({required Uri uri, Map<String, String>? headers, Map<String, String>? pathParams, Object? body, bool returnDioResponse = false, EndpointSla sla = EndpointSla.unknown})`
- `Future<HttpResponse<Map<String, dynamic>>> delete({required Uri uri, Map<String, String>? headers, Map<String, String>? pathParams, Object? body, bool returnDioResponse = false, EndpointSla sla = EndpointSla.unknown})`
- `Future<HttpResponse<Map<String, dynamic>>> put({required Uri uri, Map<String, String>? headers, Map<String, String>? pathParams, Object? body, bool returnDioResponse = false, EndpointSla sla = EndpointSla.unknown})`
- `Future<HttpResponse<Map<String, dynamic>>> multiFiles({required Uri uri, required List<IMultipartFile> fileList, required String type, List<Map<String, String>>? fields, Map<String, String>? headers, Map<String, String>? pathParams, Object? body, bool returnDioResponse = false, EndpointSla sla = EndpointSla.unknown})`
- `void addAuthInterceptor(Future<RetryResult> Function() onRetry, {required VoidCallback onForceLogout, required Future<String?> Function() getToken})` — not used by lab_results; wiring happens in the composition root.

#### ILogger — accessed via ref.watch(loggerProvider)
`ILogger` (`abstract interface class`, `lib/shared/interfaces/i_logger.dart`); bound to `loggerProvider` (`Provider<ILogger>`, impl `DevLogger`) in `lib/core/services/logging/logging_providers.dart`. Used for best-effort cache-write logging (via the `ILogger` seam, never the console).
- `void info(String message, {String? technicalMessage})`
- `void error(String message, {Object? technicalMessage, StackTrace? stackTrace})`

#### ILabResultsStore — accessed via ref.watch(labResultsStoreProvider)
ISP split in `lib/shared/interfaces/i_lab_results_store.dart` (TO BE CREATED — mirrors `IClinicalHistoryStore` pattern in `i_clinical_history_store.dart`): `ILabResultsReader`, `ILabResultsWriter`, and `ILabResultsStore implements ILabResultsReader, ILabResultsWriter`. Bound to `labResultsStoreProvider` (`Provider<ILabResultsStore>`, impl `LabResults` over sembast `StoreRef 'lab_results'`) in `lib/core/database/tables/lab_results_providers.dart` (TO BE CREATED). Adapter over it in `LabResultsLocalDatasourceImpl`. It never clears the cache — deletion only via account reset. Tests mock `ILabResultsStore`/`ILabResultsReader`/`ILabResultsWriter`.
- `Future<List<LabResultEntity>> loadAll()` — `ILabResultsReader`
- `Future<void> storeAll(List<LabResultEntity> entities)` — `ILabResultsWriter` (replaces all)
- `Future<void> deleteAll()` — `ILabResultsWriter`

#### IAppNavigator — accessed via ref.read(appNavigatorProvider)
`IAppNavigator` (`abstract interface class`, `lib/shared/interfaces/i_app_navigator.dart`); bound to `appNavigatorProvider` (`Provider<IAppNavigator>`, overridden in the composition root) in `lib/core/router/app_navigator_provider.dart`. Re-exported from the feature `di/` ONLY if the feature navigates imperatively — currently EXPECTED NOT needed (entry point lives in the clinical history AppBar action; back nav handled by the go_router shell).
- `void go(AppRoute route, {Object? extra})`
- `Future<void> push(AppRoute route, {Object? extra})`